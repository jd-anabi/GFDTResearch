"""The Parameter Inference tabs' shared matter, split out of inference_tabs.py.

inference_tabs.py remains the import surface: it re-exports everything here, and
settings_screen reads HELP + the module docstring from that path BY STRING.
"""
